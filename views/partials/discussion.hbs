<style>
      .tab-pane {
          margin: 0;
          padding-bottom: 3rem;
          font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      }

      #form {
          background: #FFFFFF;
          box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
          padding: 0.25rem;
          position: absolute;
          bottom: 0;
          display: flex;
          height: 54px;
          width: -webkit-fill-available;
          box-sizing: border-box;
          margin-right: 40px;
          margin-bottom: 30px;
      }

      #input {
          border: none;
          padding: 0 1rem;
          flex-grow: 1;
          border-radius: 2rem;
          margin: 0.25rem;
      }

      #input:focus {
          outline: none;
      }

      .send-btn {
          background: #0C8039;
          border: none;
          padding: 0 1rem;
          margin: 0.25rem;
          border-radius: 3px;
          outline: none;
          color: #fff;
      }

      .send-btn:hover {
          background: #00713D;
      }

      #messages {
          list-style-type: none;
          float: right;
          width: 80%;
          min-height: 100px;
          background: rgba(12, 128, 57, 0.6);
          box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
          padding: 10px 15px;
          margin-bottom: 20px;
      }

      #messages > li {
          padding: 0.5rem 1rem;
      
      }

      #messages > li:nth-child(odd) {
          background: #efefef;
      }

</style>

<div id = "discussion-div">
    <div class = "discussion-header">Discussion Thread:</div>
    <br>
    <ul id="messages">Sample Message</ul>
    <form id="form" action="">
      <input id="input" autocomplete="off" placeholder = "Reply here..." /><button class = "send-btn">Send</button>
    </form>

</div>

<script src="/socket.io/socket.io.js"></script>
<script>
    var socket = io();
</script>