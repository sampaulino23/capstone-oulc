<!DOCTYPE html>

<html>

<head>
    <title>Revision History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/specificrequeststyle.css">
</head>

<body>
    <div id="header">
        Revision History
    </div>
    <div class = "subheader-row row g-0">
        <div class = "col-6">
            <div class = "contract-repo-nav" id = "subheader">
                <a href="javascript:history.back()" class = "contract-repository-href">Approval Request Details</a>
                <p class = "divider" style = "display: inline;"> > </p>
                <a href = "javascript:location.reload();" id = "selected" class = "contract-repository-href">Document Revision History</a>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="">
            <div class="input-group mb-3">
                <span class="file-selected input-group-text">Contract Version</span>
                <select class="form-select" name="fileSelectedLeft" id="fileSelectedLeft" aria-label="file-selected">
                    {{#each contractversions}}
                    {{#compare ../leftcontractversion "==" _id}}
                        <option value="{{file}}" selected>Version {{version}}</option>
                    {{else}}
                        <option value="{{file}}">Version {{version}}</option>
                    {{/compare}}
                    {{/each}}
                </select>
                <select class="form-select" name="fileSelectedRight" id="fileSelectedRight" aria-label="file-selected">
                    {{#each contractversions}}
                    {{#compare ../rightcontractversion "==" _id}}
                        <option value="{{file}}" selected>Version {{version}}</option>
                    {{else}}
                        <option value="{{file}}">Version {{version}}</option>
                    {{/compare}}
                    {{/each}}
                </select>
                <button id = "compare-btn" class="mark-as-btn" type="submit">Compare</button>
            </div>
        </div>

        <div id="comparisonView">
            <iframe src="{{draftable}}"
            style="width: 100%; height: 1000px; border: 1px solid #e0e0e0;"
            allowfullscreen />
        </div>
        
    </div>

</body>

</html>

    